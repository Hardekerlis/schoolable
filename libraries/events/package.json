{
  "name": "@gustafdahl/schoolable-events",
  "version": "1.2.5",
  "description": "Events library for schoolable",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "del ./build/*",
    "build": "npm run clean && tsc",
    "patch": "npm run build && npm version patch && npm publish",
    "minor": "npm run build && npm version minor && npm publish",
    "major": "npm run build && npm version major && npm publish"
  },
  "dependencies": {
    "@gustafdahl/schoolable-enums": "^1.0.3",
    "@types/node": "^15.12.5",
    "node-nats-streaming": "^0.3.2"
  },
  "devDependencies": {
    "del-cli": "^4.0.1",
    "prettier": "^2.4.1",
    "typescript": "^4.4.3"
  },
  "author": "",
  "license": "ISC"
}
