/** @format */

// import request from 'supertest';
// import { app } from '../../../app';
//
// it('Returns a 200 on succesfully fetching active user settings', async () => {
//   // const res = await request(app).get('/api/settings').expect(200);
//   //
//   // expect(res.body.debug).toBeDefined();
// });
//
// it('Returns a 404 if the requested user settings are not found', async () => {});
//
// it('Returns a 403 on not authorized request of user settings', async () => {});
//
// it('Returns a 200 on succesfully fetching global application settings', async () => {});
//
// it('Returns a 403 on not authorized request of global application settings', async () => {});
