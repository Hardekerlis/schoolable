apiVersion: apps/v1
kind: Deployment
metadata:
  name: phase-items-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phase-items-mongo
  template:
    metadata:
      labels:
        app: phase-items-mongo
    spec:
      containers:
        - name: phase-items-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: phase-items-mongo-srv
spec:
  selector:
    app: phase-items-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
